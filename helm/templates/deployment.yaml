apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "pv-forecasting.fullname" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "pv-forecasting.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "pv-forecasting.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - containerPort: 8501
          resources:
            limits:
              cpu: {{ .Values.resources.limits.cpu }}
              memory: {{ .Values.resources.limits.memory }}
